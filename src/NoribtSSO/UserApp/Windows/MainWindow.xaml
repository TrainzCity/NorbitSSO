<Window x:Class="UserApp.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <DockPanel Background="Cyan">
            <TextBlock Style="{StaticResource TblMain}" HorizontalAlignment="Left" Text="Пользовательское приложение"></TextBlock>
        </DockPanel>
        <Frame x:Name="MainFrame" Grid.Row="1" NavigationUIVisibility="Hidden"></Frame>
        <Button Grid.Row="2" x:Name="BtnBack" Background="OrangeRed" Content="Закрыть" HorizontalAlignment="Right" Click="BtnBack_Click"></Button>
    </Grid>
</Window>
